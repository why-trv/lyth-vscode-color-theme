import type { ThemeDefinition } from "../types";

const definition: ThemeDefinition = {
  name: "Lyth Dark Mono",
  extends: "Lyth Dark",
  adjustments: {
    saturation: -1,
  },
};

export default definition;